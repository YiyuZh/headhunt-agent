# 服务器部署与使用手册

本文是 `headhunt-agent` 的云服务器本土化上线 runbook。目标是让你在一台 Linux 服务器上，把 FastAPI API、worker、PostgreSQL + pgvector、Caddy HTTPS、飞书事件/卡片回调和 Bitable 同步按同一套步骤跑通。

当前默认口径是单项目服务器：项目自带 Caddy 直接监听 `80/443`。如果你的服务器已经有统一 Caddy 网关，先看第 6 节，不要让本项目容器直接抢占公共 `80/443`。

## 1. 上线变量卡

| 项 | 值 |
| --- | --- |
| 项目名 | `headhunt-agent` |
| GitHub HTTPS | `https://github.com/YiyuZh/headhunt-agent` |
| GitHub SSH | `git@github.com:YiyuZh/headhunt-agent.git` |
| Deploy Key SSH alias | `github.com-headhunt-agent` |
| 正式域名 | `headhunt.zenithy.art` |
| 服务器公网 IPv4 | `140.143.119.81` |
| 服务器公网 IPv6 | `2402:4e00:c050:7300:c4d8:33c6:74f2:0` |
| 云服务器实例 | `Docker CE-8b5w` / `lhins-jg4o0aj0` |
| 服务器区域与规格 | 北京七区，2 核 2GB，40GB SSD，带宽 3Mbps |
| 服务器部署目录 | `/opt/apps/headhunt-agent` |
| 健康检查 | `https://headhunt.zenithy.art/health` |
| 内部就绪检查 | `https://headhunt.zenithy.art/ready` |
| 飞书事件回调 | `https://headhunt.zenithy.art/feishu/events` |
| 飞书卡片回调 | `https://headhunt.zenithy.art/feishu/card-actions` |
| 主要容器 | `lietou-api`、`lietou-worker`、`lietou-postgres`、`lietou-caddy` |

域名、DNS、`.env` 里的 `DOMAIN`、Caddy 入口必须保持一致。如果后续换服务器，再同步更新本表和 DNS A 记录。

## 2. 服务器准备

服务器建议使用 Ubuntu 22.04/24.04。先准备：

- Docker Engine 和 Docker Compose v2。
- `git`、`openssh-client`、`python3`、`curl`、`nano`。
- 云安全组/防火墙放行 `80` 和 `443`。
- DNS A 记录：`headhunt.zenithy.art -> 140.143.119.81`。
- 一个飞书开放平台应用、测试群、Bitable app。

检查基础命令：

```bash
docker compose version
git --version
python3 --version
curl --version
```

检查 DNS：

```bash
dig +short headhunt.zenithy.art
```

输出应是 `140.143.119.81`。如果没有 `dig`，可以用：

```bash
getent hosts headhunt.zenithy.art
```

## 3. 配置 GitHub Deploy Key

服务器上推荐使用项目专用 Deploy Key。不要把仓库 SSH 地址当成 Deploy Key 内容；Deploy Key 内容是 `.pub` 公钥文件里的整行文本。

生成项目专用 key：

```bash
mkdir -p ~/.ssh
chmod 700 ~/.ssh
ssh-keygen -t ed25519 -C "deploy-headhunt-agent" -f ~/.ssh/id_ed25519_headhunt_agent
cat ~/.ssh/id_ed25519_headhunt_agent.pub
```

打开 GitHub 仓库：

```text
YiyuZh/headhunt-agent -> Settings -> Deploy keys -> Add deploy key
```

把 `cat` 输出的 `.pub` 公钥整行粘进去。只部署代码时不需要勾选 write access。

然后在服务器配置项目级 SSH alias，避免覆盖多项目服务器上的全局 `Host github.com`：

```bash
nano ~/.ssh/config
```

加入：

```sshconfig
Host github.com-headhunt-agent
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_headhunt_agent
    IdentitiesOnly yes
```

设置权限并测试：

```bash
chmod 600 ~/.ssh/config ~/.ssh/id_ed25519_headhunt_agent
ssh -T github.com-headhunt-agent
```

如果看到类似下面的提示，这是成功，不是错误：

```text
Hi YiyuZh/headhunt-agent! You've successfully authenticated, but GitHub does not provide shell access.
```

再确认能读取仓库：

```bash
git ls-remote git@github.com-headhunt-agent:YiyuZh/headhunt-agent.git refs/heads/main
```

## 4. 获取代码

首次部署：

```bash
sudo mkdir -p /opt/apps
sudo chown "$USER":"$USER" /opt/apps
    git clone git@github.com-headhunt-agent:YiyuZh/headhunt-agent.git /opt/apps/headhunt-agent
    cd /opt/apps/headhunt-agent
git checkout main
```

如果服务器上已经有仓库：

```bash
cd /opt/apps/headhunt-agent
git fetch --prune origin
git checkout main
git pull --ff-only
```

记录当前版本，后续回滚会用到：

```bash
git rev-parse HEAD
git status --short
```

## 5. 生成并填写 `.env`

在项目根目录执行：

```bash
cd /opt/apps/headhunt-agent
bash scripts/lietou-oneclick.sh
```

这一步会：

- 如果 `.env` 不存在，从 `.env.example` 创建。
- 自动生成 `INTERNAL_ADMIN_API_KEY`、`POSTGRES_PASSWORD`、`MODEL_SECRET_ENCRYPTION_KEY`。
- 检查 Docker、Compose、必填环境变量和占位值。
- 不启动服务，不输出密钥值。

编辑 `.env`：

```bash
nano .env
```

至少确认这些值：

```env
DOMAIN=headhunt.zenithy.art
HTTP_PORT=80
HTTPS_PORT=443

INTERNAL_ADMIN_API_KEY=<auto-generated>
MODEL_SECRET_ENCRYPTION_KEY=<auto-generated>

POSTGRES_DB=lietou
POSTGRES_USER=lietou
POSTGRES_PASSWORD=<auto-generated>
POSTGRES_HOST=postgres
POSTGRES_INTERNAL_PORT=5432
POSTGRES_BIND=127.0.0.1
POSTGRES_PORT=5432

CHANNEL_GATEWAY_PROVIDER=feishu
FEISHU_BASE_URL=https://open.feishu.cn
FEISHU_APP_ID=
FEISHU_APP_SECRET=
FEISHU_VERIFICATION_TOKEN=
FEISHU_ENCRYPT_KEY=
FEISHU_DEFAULT_CHAT_ID=
FEISHU_BITABLE_APP_TOKEN=
FEISHU_BITABLE_REQUISITION_TABLE_ID=
FEISHU_BITABLE_CANDIDATE_TABLE_ID=
FEISHU_BITABLE_TALENT_MAP_TABLE_ID=
FEISHU_BITABLE_REPORT_TABLE_ID=

MODEL_PROVIDER_ALLOWLIST=openai,deepseek
EMBEDDING_PROVIDER=openai
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_API_KEY=
```

填写规则：

- `DOMAIN` 固定填 `headhunt.zenithy.art`，不要填 `localhost`。
- 自动生成的三个密钥保留即可，不要提交到 GitHub、聊天软件或工单。
- `POSTGRES_BIND=127.0.0.1` 表示 PostgreSQL 只暴露给服务器本机，生产环境不要改成 `0.0.0.0`。
- 用户业务模型 Key 不写进共享 `.env`。第一版主路径是用户在飞书模型配置卡里 BYOK。
- `EMBEDDING_API_KEY` 是可选的本地 embedding fallback；没填时长期记忆向量检索会被标记为未就绪。

只想预检、不创建 `.env` 时用：

```bash
bash scripts/lietou-oneclick.sh --no-init --doctor-only
```

## 6. Caddy 与网关二选一

### 方案 A：项目自带 Caddy 直连公网

这是当前默认方案，适合单项目服务器。保持 `.env`：

```env
DOMAIN=headhunt.zenithy.art
HTTP_PORT=80
HTTPS_PORT=443
```

`docker-compose.yml` 会启动 `lietou-caddy`，直接监听服务器 `80/443`，再反代到容器内 `api:8000`。

### 方案 B：服务器已有统一 Caddy 网关

如果服务器上已有统一 Caddy 或 Nginx 网关，不要直接按默认端口启动本项目。先确认：

```bash
sudo ss -lntp | grep -E ':80|:443'
docker network ls
```

需要明确三件事再改：

- 统一网关的真实 Caddyfile 路径。
- 网关是否能访问本项目 Docker network。
- 上游地址应该是容器网络名还是宿主机端口。

修改外部 Caddyfile 前先备份：

```bash
RELEASE_ID="$(date +%Y%m%d-%H%M%S)-$(git rev-parse --short HEAD)"
sudo mkdir -p /opt/apps/backups/releases/$RELEASE_ID/headhunt-agent
sudo cp /etc/caddy/Caddyfile /opt/apps/backups/releases/$RELEASE_ID/headhunt-agent/Caddyfile.before 2>/dev/null || true
```

外部 Caddyfile 改完后必须执行：

```bash
sudo caddy fmt --overwrite /etc/caddy/Caddyfile
sudo caddy validate --config /etc/caddy/Caddyfile
sudo systemctl reload caddy
```

统一网关方案需要按服务器实际网络改 Compose 暴露方式；未确认前不要让 `lietou-caddy` 和外部网关同时抢 `80/443`。

## 7. 上线前备份

每次上线或大改 `.env`、Caddyfile、Compose 前先做 release 备份：

```bash
cd /opt/apps/headhunt-agent
RELEASE_ID="$(date +%Y%m%d-%H%M%S)-$(git rev-parse --short HEAD)"
BACKUP_DIR="/opt/apps/backups/releases/$RELEASE_ID/headhunt-agent"
mkdir -p "$BACKUP_DIR"

git rev-parse HEAD > "$BACKUP_DIR/git-head.txt"
git status --short > "$BACKUP_DIR/git-status.txt"
cp .env "$BACKUP_DIR/.env" 2>/dev/null || true
cp Caddyfile "$BACKUP_DIR/Caddyfile.project" 2>/dev/null || true
docker compose config > "$BACKUP_DIR/docker-compose.rendered.yml"
```

不要执行 `docker compose down -v`。`-v` 会删除数据卷，可能直接清空 PostgreSQL 数据。

## 8. 启动前严格预检

```bash
cd /opt/apps/headhunt-agent
python3 -m app.runtime.local_doctor --strict
```

必须看到 `status` 为 `ok` 才继续。常见 blocker：

- `runtime_required still placeholder`：本地密钥没有生成，重新执行 `bash scripts/lietou-oneclick.sh`。
- `feishu_required missing`：飞书或 Bitable 必填项没填。
- `Install Docker Engine/Compose`：Docker 或 Compose v2 不可用。

还可以先检查 Compose 插值：

```bash
docker compose config --quiet
```

## 9. 一键启动

```bash
cd /opt/apps/headhunt-agent
bash scripts/lietou-oneclick.sh --start
```

这一步会先再次执行 strict preflight，通过后才运行：

```bash
docker compose up -d --build
```

如果只想复用已有镜像，不重新 build：

```bash
bash scripts/lietou-oneclick.sh --start --no-build
```

## 10. 启动后验收

查看容器：

```bash
docker compose ps
```

期望看到 `lietou-api`、`lietou-worker`、`lietou-postgres`、`lietou-caddy` 都是 running/healthy。

查看日志：

```bash
docker compose logs -f api worker caddy postgres
```

检查 HTTP/HTTPS：

```bash
curl -f http://127.0.0.1/health
curl -f https://headhunt.zenithy.art/health
curl -f -H "X-Internal-Admin-Key: <INTERNAL_ADMIN_API_KEY>" https://headhunt.zenithy.art/ready
```

容器内配置、数据库和记忆 smoke：

```bash
docker compose exec api lietou-config-check --strict
docker compose exec api lietou-postgres-smoke
docker compose exec api lietou-memory-retention --dry-run
```

单次 worker 检查：

```bash
docker compose exec worker lietou-outbox-worker --once
```

## 11. 飞书后台配置

在飞书开放平台配置：

```text
事件订阅 URL: https://headhunt.zenithy.art/feishu/events
卡片回调 URL: https://headhunt.zenithy.art/feishu/card-actions
```

注意：

- 两个回调入口使用同一组 `Verification Token` 和 `Encrypt Key`。
- `card.action.trigger` 只配置到 `/feishu/card-actions`。
- 不要同时配置旧版消息卡片请求网址或 `card.action.trigger_v1`。
- 回调保存成功不等于业务端到端成功；还要在测试群发真实任务验证卡片发送和点击。

飞书权限至少包括：

- 机器人接收群消息、发送群消息。
- 交互卡片回调。
- Bitable app/table record 读写。

## 12. 真实业务冒烟

按这个顺序试跑，不要跳过人工确认：

1. 邀请机器人进入测试群。
2. 在测试群 @机器人发一个简单职位任务。
3. 后端应快速 ACK，并在 `feishu_event_logs` 记录原始事件。
4. `feishu_outbox(kind=card_send)` 应出现任务确认卡发送任务。
5. 你在飞书卡片上 approve/reject。当前 edit 表单仍待实现，不能写成已通过。
6. approve 后才会写入 `graph_dispatch` outbox，由 worker 进入 graph。
7. 高风险副作用继续通过飞书确认卡走 HumanApproval。
8. 人工确认后才同步 Bitable。
9. 检查 Bitable 表是否出现预期记录，并核对 `feishu_bitable_write_chunks` 成功状态。

如果任何一步依赖真实飞书、真实 Bitable、真实模型或真实服务器网络，未实际跑通前都标记为“未验证”。

## 13. 常用运维命令

停止服务：

```bash
docker compose down
```

重启并重新 build：

```bash
bash scripts/lietou-oneclick.sh --start
```

拉取最新代码后重启：

```bash
git fetch --prune origin
git checkout main
git pull --ff-only
bash scripts/lietou-oneclick.sh --start
```

查看最近日志：

```bash
docker compose logs --tail=200 api worker caddy postgres
```

查看数据卷：

```bash
docker volume ls | grep postgres_data
```

## 14. 回滚

先找到要回滚到的 commit 或 tag：

```bash
git log --oneline -n 10
```

回滚代码并重新启动：

```bash
cd /opt/apps/headhunt-agent
git fetch --prune origin
git checkout <previous_commit_or_tag>
bash scripts/lietou-oneclick.sh --start
```

如果本次上线改过 `.env` 或外部 Caddyfile，从第 7 节备份目录恢复：

```bash
cp /opt/apps/backups/releases/<RELEASE_ID>/headhunt-agent/.env /opt/apps/headhunt-agent/.env
sudo cp /opt/apps/backups/releases/<RELEASE_ID>/headhunt-agent/Caddyfile.before /etc/caddy/Caddyfile
sudo caddy validate --config /etc/caddy/Caddyfile
sudo systemctl reload caddy
```

回滚后复查：

```bash
curl -f https://headhunt.zenithy.art/health
curl -f -H "X-Internal-Admin-Key: <INTERNAL_ADMIN_API_KEY>" https://headhunt.zenithy.art/ready
docker compose logs --tail=100 api worker caddy postgres
```

不要用 `docker compose down -v` 做回滚。

## 15. 故障排查

### Deploy Key 与仓库地址混淆

仓库 SSH 地址是：

```text
git@github.com:YiyuZh/headhunt-agent.git
```

Deploy Key 是服务器上 `.pub` 文件里的公钥内容：

```bash
cat ~/.ssh/id_ed25519_headhunt_agent.pub
```

多项目服务器上只配置 `Host github.com-headhunt-agent`，不要覆盖全局 `Host github.com`。

### `Could not resolve hostname github.com-headhunt-agent`

这说明还没有在服务器 `~/.ssh/config` 里配置项目级 SSH alias。`github.com-headhunt-agent` 不是公网域名，而是本机 SSH 配置里的别名。

先创建 key 和 SSH config：

```bash
mkdir -p ~/.ssh
chmod 700 ~/.ssh
test -f ~/.ssh/id_ed25519_headhunt_agent || ssh-keygen -t ed25519 -C "deploy-headhunt-agent" -f ~/.ssh/id_ed25519_headhunt_agent
touch ~/.ssh/config

grep -q '^Host github.com-headhunt-agent$' ~/.ssh/config || cat >> ~/.ssh/config <<'EOF'
Host github.com-headhunt-agent
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_ed25519_headhunt_agent
    IdentitiesOnly yes
EOF

chmod 600 ~/.ssh/config ~/.ssh/id_ed25519_headhunt_agent
cat ~/.ssh/id_ed25519_headhunt_agent.pub
```

把最后一行 `.pub` 公钥内容添加到 GitHub 仓库 `YiyuZh/headhunt-agent` 的 `Settings -> Deploy keys` 后，再验证：

```bash
ssh -T github.com-headhunt-agent
git ls-remote git@github.com-headhunt-agent:YiyuZh/headhunt-agent.git refs/heads/main
```

看到 `successfully authenticated, but GitHub does not provide shell access` 是成功。

### `ssh -T` 看起来像失败

如果输出包含：

```text
successfully authenticated, but GitHub does not provide shell access
```

这就是成功。GitHub 不提供 SSH shell，不影响 `git clone` / `git pull`。

### `POSTGRES_PASSWORD is missing`

说明 `.env` 没创建或没有被 Compose 读到。执行：

```bash
pwd
ls -la .env
bash scripts/lietou-oneclick.sh
docker compose config --quiet
```

必须在 `/opt/apps/headhunt-agent` 根目录运行启动命令。

### `status=not_ready`

看 `blocking_issues`，不要只看最后一行。缺飞书/Bitable 值时，先补 `.env`，再运行：

```bash
python3 -m app.runtime.local_doctor --strict
```

### HTTPS 证书失败

检查：

```bash
dig +short headhunt.zenithy.art
curl -I http://headhunt.zenithy.art/health
docker compose logs caddy --tail=100
```

确认域名解析到服务器公网 IP，云安全组放行 `80/443`，服务器上没有其它服务占用公共端口。

### Caddy 502

先判断你用的是项目自带 Caddy 还是统一网关：

```bash
docker compose ps
docker network ls
docker compose logs caddy --tail=100
```

项目自带 Caddy 的上游应是容器内 `api:8000`。统一网关的上游取决于网关是否加入项目 Docker network；不要盲目写宿主机 `127.0.0.1`。

### Docker build 很慢

先区分是网络慢还是依赖冲突：

```bash
docker compose build --progress=plain api
```

如果只是下载慢，可考虑按服务器网络情况配置 Docker 或 pip 镜像。如果是依赖冲突，不要只升级单个包；先看 `pyproject.toml`、`Dockerfile` 和完整错误，按一组兼容版本调整。

### 飞书回调保存失败

先确认公网访问：

```bash
curl -f https://headhunt.zenithy.art/health
```

再确认 `.env` 里的 `FEISHU_VERIFICATION_TOKEN` 和 `FEISHU_ENCRYPT_KEY` 与飞书后台一致。

## 16. Windows 本机调试入口

如果你在 Windows 本机运行：

```powershell
cd D:\apps\headhunt-agent
powershell -ExecutionPolicy Bypass -File scripts\lietou-oneclick.ps1
powershell -ExecutionPolicy Bypass -File scripts\lietou-oneclick.ps1 -Start
```

Windows 本机也要先填 `.env`，并确保 Docker Desktop 已启动。服务器上线仍以 Linux `bash scripts/lietou-oneclick.sh --start` 为准。

## 当前未验证边界

- 真实 Linux 服务器尚未执行 `bash scripts/lietou-oneclick.sh --start`。
- 真实飞书后台保存回调、真实卡片发送/更新、真实 Bitable 权限和写入，需要在服务器和飞书后台联调后才能标记为已通过。
- 真实 OpenAI/DeepSeek/embedding 调用需要用户在飞书 BYOK 卡片里保存模型后验证。
- 真实 PostgreSQL checkpointer 在线 interrupt/resume 需要服务启动后走完整 approve/resume 链路验证。
